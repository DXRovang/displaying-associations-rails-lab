<ul>
<% @artists.each do |artist| %>
<li><%= artist.name %> - <%= pluralize(artist.song_count, 'song') %>
<% artist.songs.each do |song| %>
<p><%= artist.name %> - <%= link_to song.title, song_path(song) %></p></li>
<% end %>
<% end %>
</ul>
